/* import React, { Component } from 'react'
import PropTypes from 'prop-types'

class MindmapEdges extends Component {

    state = {
        edgeData: this.props.edgeData
    }

    static propTypes = {
        edgeData: PropTypes.object
    }

    render() {
        return (
            {edges}
        )
    }
}

export default MindmapEdges; */

/* MAYBE NOT NEEDED */